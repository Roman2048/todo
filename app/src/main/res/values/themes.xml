<resources>

    <style name="Theme.Planner" parent="Theme.MaterialComponents.Light">
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
    </style>

    <style name="Theme.PlannerSplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/primaryColor</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_baseline_add_task_24</item>
        <item name="windowSplashScreenAnimationDuration">200</item>
        <item name="postSplashScreenTheme">@style/Theme.Planner</item>
    </style>

    <style name="AddTaskBottomSheetStyle" parent="Theme.MaterialComponents.Light.BottomSheetDialog" />

</resources>