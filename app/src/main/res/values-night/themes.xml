<resources>

    <style name="Theme.Planner" parent="Theme.MaterialComponents">
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
    </style>

    <style name="Theme.PlannerSplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/primaryColor</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_ld_planner_splash_icon</item>
        <item name="windowSplashScreenAnimationDuration">200</item>
        <item name="postSplashScreenTheme">@style/Theme.Planner</item>
    </style>

    <style name="AddTaskBottomSheetStyle" parent="Theme.MaterialComponents.BottomSheetDialog" />

</resources>